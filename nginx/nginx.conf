http {
    events {
        worker_connections  1024;  ## Default: 1024
    }
    server {
        listen 80 default_server;
        listen [::]:80 default_server;
        root /wwwroot/public;

        location / {
            try_files $uri /index.html;
        }
    }
}
